# Level Up – Backend

Backend for Level Up (auth, API, optional sync) will be added in a future phase. V1 of the app is client-only and uses local storage.

**Planned later:** API, database (e.g. PostgreSQL or MongoDB), user accounts, and cloud sync.
